<%= render 'shared/header', header: "Timeline" %>
<ul>
<% @statuses.each do |status| %>
  <li><%= status.body %> by <%= link_to '@'+status.creator.username, user_path(status.creator.username) %> at <%= status.created_at %> </li>
<% end %>
</ul>
